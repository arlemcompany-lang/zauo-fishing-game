# CLAUDE.md — ざうおスマホゲーム

## プロジェクト概要
飲食店「ざうお目黒店」の夏休み限定・子供向けブラウザ釣りゲーム。
来店した家族の子供が店内でスマホ(QRコード経由)で遊ぶ。
あつまれどうぶつの森風の見下ろし2D。店内マップを歩き回って魚を釣り、図鑑をコンプリートする。

- 詳細仕様は `spec.md` を参照(クライアントとの壁打ちで作成した元仕様書)
- 技術構成: HTML/CSS/JavaScript、`index.html` 1ファイル完結(Canvas描画、外部ライブラリなし)
- 対象: 小学生前後の子供(インバウンド客の子供も含む)
- 対応言語: 日本語/英語/中国語/韓国語/スペイン語/イタリア語/ベトナム語/タガログ語の8言語(元仕様書は4言語だが、オーナー判断で追加していき8言語に)
- 期間: 夏休み限定イベント用

---

## 最重要要件(プライバシー)
- **データは一切保存しない**。localStorage / sessionStorage / Cookie / IndexedDB / サーバー送信すべて禁止
- 名前・キャラ選択・図鑑の進捗はすべてJS変数のみで保持。リロードや画面を閉じると完全消滅
- 名前入力はネイティブの `<input>` を使わず、画面上のオリジナル五十音表/ABC表タップ方式で実装済み
  (端末のキーボード履歴・予測変換に一切残らないようにするため。仕様書の `autocomplete="off"` 案より安全な実装として採用)

---

## 現在の進捗

### 実装済み(フェーズ1: `index.html`)
- **言語切り替えは上部の国旗バー(`#langBar`)**。魚種判別アプリと同じ方式にオーナー指示で変更(2026-08-16)
  - **日本語で始まる**(`state.lang`の初期値が`ja`)。最初の画面はキャラメイク
  - 全画面共通で上部に固定表示。**国旗をタップしてもその場で言語が変わるだけで画面遷移はしない**(`setLang()`)
  - 名前入力中に切り替えた場合は五十音表⇔ABC表を組み替え、入力済みの名前はクリアする
  - 「さいしょから」の戻り先はキャラメイク。**選んだ言語は保たれる**
  - 国旗はすべてCanvas自前描画(絵文字だとWindows等で正しく表示されない機種があるため)
  - 旧: 8言語を大きなボタンで選ばせる専用画面(`screen-lang`)。廃止済み。
    廃止時に`.lang-grid`/`.lang-btn`の旧CSSが`#langBar`のボタンに効いて見た目が崩れたので、
    **画面を消すときはその画面専用のCSSも一緒に消すこと**
- キャラメイク画面
  - Canvas描画のみでキャラを生成(画像素材なし。パーツ分けして色をコードで置換する方式)
  - 性別(男の子/女の子)、髪色4色、肌色4段階、服色3色(海テーマ)
  - 髪型は男女で別リスト: 男の子=坊主/スポーツ刈り/ミディアム/クルクルヘアー/ストレートロング、女の子=ショートヘア/お団子/二つ結び/一つ結び/ストレート/クルクルヘアー(性別切替時は各リストの先頭にリセット)
  - 各選択肢はミニプレビューCanvasで表示(文字が読めなくても選べる)
- 名前入力画面
  - 日本語: 五十音表(清音+濁音/半濁音+促音・拗音・長音)をタップ入力
  - 日本語以外: ABC表をタップ入力(スペイン語/イタリア語もアクセント記号なしの簡略ABC表を共用)
  - 最大6文字、スキップボタンあり
  - 音声入力ボタン(🎤): `window.SpeechRecognition`対応ブラウザにのみ表示(機能検出、`updateVoiceButtonVisibility()`)。**iPhone SafariはWeb Speech API非対応のため非表示になる**(Android Chrome/PC Chromeのみで使える仕様)。認識結果はJS変数に直接反映するのみで保存はしない。音声はブラウザ標準機能でクラウド認識(Chromeの場合Google)に送られる点はプライバシー要件と別軸の留意点としてオーナーに説明済み
- スタート確認画面: キャラ+名前の確認表示 →「たんけんへ!」でマップ画面へ

### 実装済み(フェーズ2: プレースホルダーマップ)
- タイルグリッドマップ(`index.html` 内 `buildMapGrid()`): 中央デッキ(DECK)+周囲水路(WATER)+外周通路(AISLE)+南北の橋(BRIDGE)+カゴ4個(KAGO)。distance-from-edgeで機械生成しているため、間取り図が届いたら配列を差し替えるだけで本番マップに移行可能
- タップ移動: BFS経路探索+マス送りアニメーション、壁・水路・カゴは通行不可(当たり判定OK)
- 水路に隣接すると「つる」ボタン、カゴに隣接すると「のぞく」ボタンが出現(タップするとトースト「ミニゲームはじゅんびちゅう」表示。実際のミニゲームはフェーズ3で実装)
- マップ画面下に「さいしょから」ボタン

### 実装済み(フェーズ3: ミニゲーム+図鑑)
- 釣りミニゲーム(`screen-fish`): 「つる」タップ後、ウキが2〜6秒待って沈む→「！」表示中(0.9秒間)にタップで「掛ける」ことに成功。早すぎ/遅すぎは失敗メッセージ。
  - **2026-08-16に操作方法を変更(オーナー指示)**。「連打」から「リールを回す」方式にした
    - あたりが来たら**画面のどこをタップしてもよい**(ウキを狙わせない)。イベントは`#screen-fish`全体で拾い、
      下部ボタンとリールの上だけ除外している
    - 掛かると**「ヒット！」を大きく表示**(`drawHitScene()`、0.8秒。波紋つき)。魚の種類はこの瞬間に抽選する
    - そのあと**リールの円(`#reelDial`)が出る**。指でぐるぐるなぞると巻き上がる
      - **巻いている間も海は消さない**(オーナー指示)。海の絵と円を縦に並べて両方見せる。
        `#screen-fish.reeling`が付いている間だけ海の絵を170pxに縮め、その残りを円に回す
      - **円の大きさは`layoutReel()`が毎回その場で測って決める**。
        「海の絵と下のボタンを残した残りの高さ」と「画面幅」の小さいほうに合わせる(150〜330pxで頭打ち)。
        端末ごとに使える高さが違うのでCSSの固定値にはしていない。実測: 360x640で236px / 412x915で330px
      - 円の中央には**巻き上がりの%**を出す。魚のシルエットは上の海の絵が受け持つので重複させない
      - 表示を大きくしたぶんボケないよう、`layoutReel()`が表示サイズ×DPRで描画解像度も決める
      - 指の角度の変化量を積算し、1回転(2π)で`FISH_FIGHT.gain`ぶんゲージが増える
      - **回す向きはどちらでもよい**(子供が迷わないよう、角度差の絶対値で加算している)
      - 指を離している間は回す向きの矢印が点滅して操作を誘導する
      - `pointerdown/move/up` + `setPointerCapture`で、なぞる指が円の外に出ても続けられる
    - 魚が引く力でゲージが減る仕組み(`currentDecayRate()`のsin波)は従来どおり
  - **難易度(巻く回数)もオーナー指示で大幅に上げた**。連打式は1秒に何回も入力できる前提の数字だったが、
    リール式は指の回転速度(1〜2回転/秒)が上限になるため`FISH_FIGHT`ごと組み直し、開始ゲージ25%・時間切れ30秒にした
    - **開始ゲージは10%**(オーナー指示。当初40%→25%→10%)
    - Playwrightで実測した必要回転数(10%→100%): 1回転/秒で **アジ17 / カサゴ21 / タイ31 / ヒラメ27 / シマアジ39.5** 回転
      (速く巻けば16〜30回転)。連打式だった頃は2〜3回転
    - **巻きはじめの1.2秒は魚が引かない**(`REEL_GRACE_MS`)。開始が10%と低いため、
      指を円に置くまでの一瞬でゲージが0になり即失敗する事故を防ぐ。
      実測: 一番引きが強いシマアジでも、何もせず4秒待ってまだ3%残る
    - 時間切れは90秒。`base < gain`なので巻き続けている限りゲージは必ず前に進む。
      時間切れは「手が止まった子」への区切りという位置づけで、
      ゆっくり巻いた実測の最長(シマアジ約50秒)に対して十分な余裕を取ってある
    - **`base`は`gain`より十分小さく保つこと**。`base`が`gain`に近いとゆっくり巻く小さい子が永久に釣れなくなる
      (一度`base`を上げすぎてシマアジが取れない状態を作ってしまった)
  - 掛けた瞬間に`FISH_TABLE`(アジ45/カサゴ25/タイ12/ヒラメ12/シマアジ6%)から釣れる魚を`weightedPick()`で内部抽選し(この時点では画面には見せず、シルエットのみ表示)、「引き寄せ」フェーズ(連続タップでゲージを貯める駆け引き)に移行
  - 引き寄せ中はゲージが自動的に増減する。減少速度は固定値ではなく`FISH_FIGHT`の`base`(平均の引きの強さ)±`amp`(振れ幅)をsin波で時間変化させており(`currentDecayRate()`)、アジ(amp:0)は一定速度、タイ/ヒラメ(amp:18≥base:14)は強い引き(ゲージ急減)と一瞬のゆるみ(ゲージが自然に少し戻ることもある)を繰り返す。連打するたびに`gain`値ずつゲージが増え、100%到達で釣り上げ成功、0%まで落ちる(または10秒タイムアウト)と「つよくひっぱられて、にげられちゃった…」で失敗。レア魚ほどamp・periodが大きく強弱の波が激しくなり駆け引きが難しい。強い引きの瞬間はゲージバーが赤くなり魚のシルエットも大きく揺れる視覚フィードバックあり
  - 成功時に図鑑登録。「もういちど」「もどる」ボタンあり
  - i18nキー`hitMsg`(ヒット！)を8言語ぶん追加。`reelMsg`は「れんぞくタップ！」→「ぐるぐる まわして！」に変更
- 宝探しミニゲーム(`screen-peek`): 「のぞく」タップ後、水底に光るポイント(`.glow-spot`)が3箇所出現。タップで抽選。イセエビ獲得時は`goldFlash()`で画面が金色に光る演出
  - **カゴは2種類**(オーナー指示 2026-08-16。実店舗ざうおの生け簀のカゴに合わせ、狙って獲れるようにした)
    - `'e'` エビカゴ(`T.KAGO_EBI`)… クルマエビ80 / イセエビ20(レア)
    - `'s'` 貝カゴ(`T.KAGO_KAI`)… サザエ85 / アワビ15(レア)
  - **アワビを新規追加**(オーナー指示 2026-08-16)。貝がサザエ1種だけだと貝カゴに当たりがなく狙う楽しみがないため。
    図鑑は8種→9種(3×3でちょうど1画面に収まる)。イセエビと同じく獲得時に`goldFlash()`が出る当たり枠(`RARE_TREASURES`)
  - アワビの絵は`SPECIES_INFO`に新しい`type:"abalone"`を追加して描いている。
    平たい耳型の殻+ふちに並ぶ穴+真珠光沢で、サザエ(オレンジの渦巻き)と一目で見分けられる
  - 抽選表は`TREASURE_TABLES`にカゴ種別ごとに持つ。隣接したカゴの種類を`state.kagoType`に覚えて`handlePeekTap()`が参照する
  - マップ上のカゴは種類ごとに描き分け(エビカゴ=茶色に赤いエビ / 貝カゴ=灰茶色に白い渦巻き)。文字を使わないので言語に依存しない
  - **配置はオーナーの手描き図(2026-08-16)どおり**。カゴはすべて歩ける場所に面した水のマスに置く
    - 上の大水槽の左端の列: 貝カゴ(上) / エビカゴ(下) … どちらも通路から届く
    - 中央の海の通路側、橋のすぐ手前: 貝カゴ
    - 橋のすぐ下、船のとなり: エビカゴ
  - 種類を増やすときは`TREASURE_TABLES`に1行足し、あわせて`FISH_NAMES`(8言語)/`SPECIES_INFO`/
    `COLLECTION_ORDER`/`drawSpeciesGraphic`に追加する。図鑑のコンプリート判定は`COLLECTION_ORDER`基準で自動追従する
- 図鑑画面(`screen-zukan`): マップ下の「ずかん」ボタンからいつでも入れる。全8種を`drawSpeciesIcon()`でCanvas自前描画(魚型+ヒラメは平たい形+サザエは渦巻き+エビ2種、カサゴ・イセエビは背びれ/トゲの`spiny`装飾あり)、未獲得はシルエット(グレー)+「？？？」表示。全種コンプリートで紙吹雪演出(`launchConfetti()`)+お祝いメッセージ(`COLLECTION_ORDER`の内容で自動判定するため種類数が変わっても追従)
- 図鑑進捗(`state.collected`)は名前・キャラ同様JS変数のみ。「さいしょから」で最初の言語選択に戻ると同時にリセットされる(保存禁止要件を維持)
- Playwrightで自動操作テスト済み(釣り成功/失敗、宝探し、イセエビ金演出、図鑑コンプリート演出、8言語のi18nキー整合性)。JSコンソールエラーなし

### 実装済み(カゴの中身を見せる演出、2026-08-16)
- `playPeekReveal()`を追加。**従来は「◯◯ ゲット！」の文字だけで、貝やエビの姿が一度も見えなかった**
  (魚には`playCatchReveal()`があるのに宝探し側にはなかった)
  - 水底から獲物がゆっくり浮かび上がり、あぶくときらきらを伴って中央で見得を切る(約1.15秒)
  - `TREASURE_RARITY`(サザエ1/クルマエビ2/アワビ3/イセエビ4)で演出の派手さが変わる
  - 当たり枠(`RARE_TREASURES` = アワビ・イセエビ)は`goldFlash()`も発動。金色フラッシュの発火は
    `handlePeekTap()`から演出側に移し、獲物が現れる瞬間と合うようにした
  - 演出が終わってから結果の文字と「もういちど/もどる」を出す
- **クルマエビは白・イセエビは赤**(オーナー指示)。さらに`SPECIES_INFO`に`scale`を追加して
  クルマエビだけ0.78倍で小さく描くようにし、色と大きさの両方で見分けられるようにした。
  `scale`は`drawSpeciesGraphic()`の冒頭で効くので、図鑑・釣り上げ演出・カゴの演出すべてに反映される
  - 白い体は図鑑の白いカードの上で埋もれるため、輪郭(`accent`)は濃いめのグレーにしてある
- **エビの絵を描き直した**。従来は赤い塊にトゲが数本生えただけで、拡大するとエビに見えなかった。
  円弧に沿って節を並べ、尾びれ・目・ヒゲ・脚を付けた形に変更(イセエビは背中のトゲとヒゲが太い)。
  図鑑の小さいアイコンでもクルマエビとイセエビが見分けられることを確認済み

### 実装済み(釣り上げ演出)
- 釣り成功(`endFishing(true, ...)`)時に、水面から魚が跳ねて色付きの姿を見せる`playCatchReveal()`アニメーション(約1.3秒)を追加。従来は成功してもウキの静止画のままで魚の姿が一切表示されなかった問題を解消(spec.mdの「魚が跳ねる演出」要件に対応)
  - 水しぶきの輪(リング)+水滴パーティクルが跳ねた瞬間に飛び散る
  - 魚は`drawSpeciesGraphic()`をシルエットではなく実カラーで使い、放物線を描いて跳び上がり、着地後は中央で少し揺れながら見得を切るポーズになる
  - `FISH_RARITY`(アジ1〜シマアジ4)でジャンプの高さ・キラキラの数を段階的に派手にし、レア度が高いほど演出が豪華になる
  - 最レア(シマアジ、rarity4)は跳躍時に既存の`goldFlash()`(イセエビ演出と同じ画面フラッシュ)も追加発動
  - 演出完了後に「◯◯ ゲット！」メッセージと「もういちど/もどる」ボタンを表示する流れは維持
- Playwrightで全レア度(アジ/カサゴ/タイ/シマアジ)の演出+実際のマップ移動→つる→掛ける→引き寄せの一連の本物のゲームフローを検証、JSコンソールエラーなし

### 実装済み(フェーズ4: 本番マップ差し替え、2026-07-28)
- 店舗名を「ざうお新宿店」から正式な「ざうお目黒店(ZAUO MEGURO)」に修正(index.htmlタイトル/spec.md/CLAUDE.md)。オーナーから届いた間取り図の店舗名表記に合わせた
- 間取り図(2026-07-28受領、`基本計画平面図 1/150`)を元に`buildMapGrid()`をASCIIアートの`MAP_ROWS_DATA`配列方式に置き換え(9列(内側)×48行の縦長マップ、`T`は`WALL/AISLE/DECK/WATER/KAGO`の5種)
  - 水場は間取り図通り2箇所実装: 上部(奥・階段側)の大水槽と、下部(入口・レジ側)の蛇行する水路+舟形の生け簀(「船上座敷席」に相当)。どちらも「つる」(釣り)の対象
  - 「のぞく」(宝探し)スポットは下部の蛇行水路沿いに4箇所配置(実店舗の生け簀カゴではなく、船上座敷席から水面をのぞくイメージ)
  - 厨房・バックヤードはWALL(歩行不可)、テーブル席・座敷席エリアは全面AISLE/DECK(歩行可)として実装(ユーザー確認済み方針)
  - マップが縦に長い(48行)ため、プレイヤー追従のカメラ(`cameraY`)による縦スクロール描画を追加。表示ビューポートは11列×15行固定、クリック座標もカメラオフセット込みで判定
  - 最上段にプレイヤーが来た際にキャラ画像が画面上端で欠けるバグを発見・修正(上端に歩行不可の余白行を2行追加するバッファ方式で解決)
  - 「このマップはかりのものです」のプレースホルダー注記(8言語分の`mapPlaceholder`キー含む)は本番マップ化に伴い削除
  - Playwrightで検証: 全体の到達可能性(上部水槽/下部水路/のぞくスポットすべてBFSで到達可能)、つる/のぞくボタンの表示、釣り画面・宝探し画面への遷移、JSコンソールエラーなし

### 実装済み(フェーズ5準備: スマホ実寸レイアウト修正、2026-08-15)
実機テストの前段として、Playwrightで実機相当の縦持ちビューポート(375x667 / 360x640 / 390x664 / 412x915)を
エミュレートしてレイアウトを検証したところ、**PC幅では気づけない致命的な崩れが2件**見つかったので修正した。

- **名前入力の五十音表が幅18pxまで潰れていた(最重要)**
  - 原因: `#kbArea` は `.screen`(flexカラム + `align-items:center`)の子だが幅指定がなく、
    中の `.kb-grid{width:100%}` が親の幅を参照 → 親は中身基準で縮む、という循環でmin-content幅(=文字1つ分)に収縮していた
  - 修正: `#kbArea{width:100%; display:flex; flex-direction:column; align-items:center;}` を追加
  - 結果: キー寸法が 18x47px → 64x47px(375px幅時)。子供の指でも押せるサイズになった
  - **これはフェーズ1からずっと壊れていた**。PC幅でも同様に潰れていたが、スクリーンショットを実寸で見るまで気づけなかった
- **「はじめる」「つぎへ」ボタンが画面外にあった**
  - 名前入力画面は全体の高さが1189px(iPhone SE の表示領域は667px)、キャラメイク画面は1110px あり、
    `.screen{overflow-y:auto}` による画面全体スクロールで下端まで下げないとボタンに到達できなかった
  - 修正: **スクロールさせる範囲を中身だけに限定**する構成へ変更
    - 名前入力: `#kbArea` を `flex:1; overflow-y:auto` にし、タイトル/名前表示/けす・スキップ/はじめる は固定
    - キャラメイク: 6つの `.option-block` を新設の `<div id="charOptions">` で囲んで同様にスクロール領域化し、
      タイトル/キャラプレビュー/つぎへ は固定
  - 併せて `@media (max-height:720px)` でキャラプレビューを 180x220 → 148x181 に縮小し、
    短い端末でも選択肢が多く見えるようにした(選択肢欄の可視高さ 254px → 305px @360x640)
- 検証結果(4サイズ全て): つぎへ/はじめる/もどる が常時画面内、画面全体スクロールなし、キー寸法36px以上、JSエラーなし
  - 五十音キーの実タップ→文字入力、釣り画面への遷移と復帰も自動テストで確認
  - タッチ(指スワイプ)でのスクロールがChromium実機エミュレーションで効くことも確認済み
    (`body{touch-action:none}` があるため念のため検証した。ただし**iOS Safariでの挙動は実機確認が必要**)

### 実装済み(手描き店内図でマップ全面作り直し、2026-08-15)
フェーズ4で使った基本計画平面図(1/150)では店内の構造を読み違えていたことがオーナー指摘で判明し、
手描きの店内図を受領してマップを全面的に作り直した。**フェーズ4のマップは破棄**。

- 新しい店の形は「上が細く下が広い」逆L字。列の役割は上から下までほぼ共通(12列(内側)×32行)
  - 左3列: 左の通路(中〜下のみ。上部は店の外)
  - 次の2列: 海。中央を縦に貫く
  - 次の4列: 上〜中は通路(北の突き当りから南への一本道)、下はそのまま船(船上座敷席)になる
  - 右3列: 上の大水槽(海)。**この列は上部にしか存在せず右にはみ出す**(オーナー指示)
- **橋**が中央の海を東西に渡し、左の通路と右側の通路をつなぐ。**これが左半分への唯一の道**
- **船は席だが特別に歩行可**(オーナー判断)。「?」と書かれていた区画は席ではなく通路と判明し、突き当りまで通路にした
- **スタート地点は左下の通路の一番下(＝入口・レジ。オーナー確認済み)**。座標はハードコードせず
  `findStartTile()` が最下段から上へ・各行は左から右へ探して最初の歩けるマスを返す方式にした(マップを書き換えても追従する)
  - 入口から一番遠い場所(上の突き当り)まで50歩。タップ移動なので1タップで行ける
- **船をマスの集まり(四角い板)ではなく1隻の船として描くようにした**(`drawBoat()`)
  - 船尾(上端)は通路と地続きなので直線、**船首(下端)は左右の舷から中心線上の1点へ向かうベジェ曲線**で
    2.2マスぶん伸ばしてとがらせる。DECKタイルの範囲(`boatGeometry()`)から寸法を求めるのでマップを変えても追従する
  - **船首は必ず中心線で左右対称に描くこと**。当初はDECKタイルの各行の左右端をなぞって輪郭を作っていたが、
    マス目のすぼまり方が左右で1マスずれるため先端がゆがみ、船に見えなかった。
    歩ける船はマス目どおりの四角のままにして、とがった船首は計算で描く方式に変更した
  - 見た目はオーナー提供の木造ボートの俯瞰イラストを参考にした。船底の木目グラデーション、
    ぐるりと一周する明るい**ふなべり(縁)**、横に渡した**腰掛け板**(船の長さから枚数を自動計算)、
    船の長さ方向に走る甲板の板目、船のまわりの喫水線(白いふくらみ)
  - ふなべりは「輪郭のパスで切り抜いてから、その輪郭を太い線で2回(濃い色→明るい色)なぞる」方式で内側にだけ縁を作っている
  - **船と水路の長さはオーナー指示で短くした**(船は17行→9行、幅4列で約2:1。参考イラストのずんぐりした比率に合わせた)。
    マップ全体も45行→32行になり、大きい端末(412x915)ではスクロールなしで全体が見えるようになった
  - **マップの船の形を変えてもDECKタイルから輪郭を作り直すので追従する**(座標のハードコードなし)
  - DECKタイル自体は「水」として描き、その上に船体を重ねている(船が水に浮いて見えるようにするため)
  - 船の右側にも海を1列通した。壁に接していると船が浮いて見えないため
- **橋を専用タイル`'b'`(`T.BRIDGE`)に分離**。船と同じ`'d'`だと`drawBoat()`が橋まで船として描いてしまう。
  橋は渡る向き(東西)と直交する板目 + 南北の縁に手すりを描いて船と見分けられるようにした
- 操舵室の角丸に`ctx.roundRect`を使うと**古いiOS Safari(16.3以前)で落ちる**ため、
  ファイル内に元からある自前の`roundRect()`ヘルパーを使っている。今後Canvas描画を足すときも同様に注意
- タイルサイズを26→24pxに縮小(列が9→12に増え、360px幅の端末でキャンバスがはみ出すため)
- **表示行数を画面高さに応じて可変にした**(`fitViewRows()`)。従来は15行固定で、大きい端末では画面の
  1/3以上が余り、小さい端末では下のボタンを押し出していた。画面回転・アドレスバーの出入りにも`resize`で追従
  - 実測: 360x640→22行 / 375x667→23行 / 412x915→34行
- 検証(4サイズ): 行の長さが全行そろっている、歩けるマス254個すべてBFSで到達可能、左の通路→橋→船の
  経路が通っている、つる/のぞくスポットに歩いて到達できる、キャンバスが画面幅に収まる、JSエラーなし
  - 検証スクリプトは「孤立した海」も報告するが、上の大水槽の外側2列と船首の水は
    **意図的に歩行マスと隣接していない**(大きな水槽の奥側なので手前からしか釣れない)。NG表示は想定内

### 公開状況
- **2026-08-16 に公開URLを最新化済み**(コミット`86dd368`)。
  https://arlemcompany-lang.github.io/zauo-fishing-game/
  - それまでフェーズ4以降が未pushで、公開されていたのはプレースホルダーマップ版だった
  - 公開後に実URLへアクセスして検証済み: 国旗バー8個 / 図鑑9種 / クルマエビ表記 / 新マップ32行 /
    船の描画 / カゴの演出 がすべて反映され、マップ画面まで通ってJSエラーなし

### 未実装(今後)
- [ ] フェーズ5: 実機テスト(iPhone Safari / Android Chrome)、店舗テスト

### 既知の課題
- 名前入力の五十音表(16行・850px)は狭い端末だとキーボード枠内(約280〜330px)を縦スクロールする必要がある。
  濁音・小文字まで使うにはスワイプが要る。押しにくくはないが、店舗テストで子供が気づけるか要観察

---

## 確定仕様(spec.mdより抜粋)

### 釣れる魚(出現率)
※オーナー判断でspec.md原案から変更。シマアジを★★→★★★★(最レア)に格上げ、空いた★★にカサゴを追加(全5種に)
| 魚 | レア度 | 出現率 |
|---|---|---|
| アジ | ★ | 45% |
| カサゴ | ★★ | 25% |
| タイ | ★★★ | 12% |
| ヒラメ | ★★★ | 12% |
| シマアジ | ★★★★ | 6% |

### 宝探しで獲れるもの(出現率)
※オーナー指示でカゴを2種類に分け、狙って獲れるようにした(2026-08-16)。あわせてアワビを追加
| カゴ | 獲物 | 出現率 |
|---|---|---|
| 貝カゴ | サザエ | 85% |
| 貝カゴ | **アワビ** | 15%(獲得時は画面が金色に光る特別演出) |
| エビカゴ | クルマエビ | 80% |
| エビカゴ | イセエビ | 20%(獲得時は画面が金色に光る特別演出) |

### 多言語対応
- 翻訳テキストは `index.html` 内の `I18N` オブジェクトに集約
- 魚介8種の名前は `FISH_NAMES` オブジェクトに集約(8言語)

---

## 日本語表記のルール
- **画面に出る日本語に漢字を使わない**(オーナー指示 2026-08-16。対象が小学生前後のため)
  - 魚介の名前はカタカナ(クルマエビ / イセエビ / アワビ など)。旧「車海老」はカタカナに変更済み
  - UI文言はひらがな(「つる」「のぞく」「ずかん」「さいしょから」など。もともと漢字なし)
- 例外は `<title>` の店名「ざうお目黒店」のみ。**正式な店名なので漢字のまま残している**
  (タイトルはブラウザのタブに出るだけで、ゲーム中の画面には出ない)
- 日本語を追加したら、`I18N.ja` と `FISH_NAMES` の `ja` に漢字が混ざっていないか確認すること

## デザイン方針
- 縦持ちスマホ前提、タッチ操作のみ(キーボード入力なし、バーチャルパッドも使わない)
- 画像素材は使わずCanvas/SVGでコード生成(外部ファイル依存を減らす)
- iPhone Safari / Android Chrome 両対応が必須
